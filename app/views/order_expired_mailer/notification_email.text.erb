Las siguientes ordenes se encuentran vencidas!
===============================================

Ordenes:

<% @expire_orders.each_with_index do |order, index| %>
  <% user = @customers[index] %>
  Nombre del cliente: <%= "#{user.name} #{user.last_name}" %>
  Email: <%= user.email %>
  Telefono: <%= user.phone_number %>
  Fecha de creación: <%= DateTime.parse(order['date']).to_formatted_s(:long) %>
  Tiempo de llegada maximo: <%= DateTime.parse(order['scheduled_date']).to_formatted_s(:long) %>
  Rango horas de llegada:  <%="#{Time.parse(order['arrival_time']).to_formatted_s(:time)} - #{Time.parse(order['expiry_time']).to_formatted_s(:time)}" %>
  ---------------------------------------------------------------

<% end %>


Fecha de generación: <%= DateTime.current.to_formatted_s(:long) %>cc