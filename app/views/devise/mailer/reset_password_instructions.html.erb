<p>Hello <%= @resource.email %>!</p>

<p>Someone has requested a link to change your password. You can do this through the link below.</p>

<a href=<%="localhost:4567/change-password.html?reset_password_token=#{@token}"%> > Cambiar contraseña</a>

<p> <%="www.frepi.co/change-password.html?reset_password_token=#{@token}"%> </p>

<p> <%="localhost:4567/change-password.html?reset_password_token=#{@token}"%> </p>

<a href=<%="frepi.co/change-password.html?reset_password_token=#{@token}"%> > Cambiar contraseña</a>

<p>If you didn't request this, please ignore this email.</p>
<p>Your password won't change until you access the link above and create a new one.</p>
